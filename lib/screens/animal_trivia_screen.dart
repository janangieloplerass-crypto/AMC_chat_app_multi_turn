import 'package:flutter/material.dart';
import 'base_topic_screen.dart';

class AnimalTriviaScreen extends StatelessWidget {
  const AnimalTriviaScreen({super.key});

  @override
  Widget build(BuildContext context) => const BaseTopicScreen(
    topic: "Animal Trivia and Facts",
  );
}